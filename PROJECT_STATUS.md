# מצב הפרויקט — 14/09/2026

## עבודה בשלבים
המשתמשת אינה מפתחת. הסברים בעברית פשוטה. לממש רק את השלב המבוקש.
Next.js 15, TypeScript ללא any, Tailwind v4, Supabase Auth/Postgres/Storage/RLS, Anthropic SDK, Vercel.
כל הממשק עברית ו־RTL; תאריכים DD/MM/YYYY; סכומים בשקלים numeric(12,2).
כל שאילתה פרטית מסוננת לפי user_id, ובפרופיל לפי id של המשתמש. אין להוסיף ספריות ללא בקשה.
אין לשמור מפתחות או קובצי סביבה אמיתיים ב־Git. יש רק תבנית ריקה .env.local.example.

## מה קיים
- אימות משתמשים, מעטפת, הגדרות עסק.
- העלאת מסמכים וזיהוי דרך Anthropic, מסך אישור והזנה ידנית.
- מסכי /transactions ו־/calendar, מסננים, מיון, עימוד, סיכומים וחלונית פרטים משותפת עם עריכה ומחיקה.
- הטעינה מתבצעת בשרת רק לטווח הנבחר, במנות של 500 כדי להתגבר על מגבלת תשובת Supabase. סינון נוסף ומיון בשרת; לדפדפן מועברות 50 תנועות לעמוד.
- הסיכומים כוללים תנועות מאושרות בלבד. הכנסות והוצאות כוללות מע״מ. מע״מ נטו = מע״מ הכנסות פחות מע״מ הוצאות לפי אחוז ההכרה.
- מספר מסמכי החודש מחושב לפי uploaded_at באזור הזמן ישראל, בנפרד מתאריך התנועה.

## בדיקות ומגבלות
- node scripts/test-transactions.cjs עבר: חישובי תקופות, תאריכים, סיכומים, עימוד, סינון בעלות ובדיקות עריכה ומחיקה מול תחליף מקומי למסד הנתונים.
- npm run build עבר עם בדיקות טיפוסים ולינט.
- בדפדפן נבדקה הפניה להתחברות בלבד; אין עדיין בדיקה ידנית מלאה של המסכים החדשים עם משתמש מחובר או בטלפון. המשתמשת תבדוק באתר החי.
- מחיקה חסומה אם מסמך משותף לתנועות נוספות. מחיקת Storage ומסד הנתונים אינה פעולה אטומית; הודעות השגיאה מבחינות בכשל חלקי ומאפשרות ניסיון נוסף.
- עריכה משתמשת ב־updated_at למניעת דריסת שינוי מקביל.
- מנוע החילוץ הקודם מגביל מקביליות ברמת תהליך, לא גלובלית ב־Vercel. HEIC אינו נתמך בחילוץ.
- SPEC.md הוא אפיון מוקדם; דרישות המשתמשת המאוחרות וקוד הפרויקט עשויים להיות שונים ממנו.
- דשבורד עם נתונים וייצוא לרו״ח טרם נבנו.

## מסד הנתונים
supabase/schema.sql הוא בסיס ההקמה. המשתמשת הריצה בהצלחה את migration_2_relax_transaction_dedupe.sql והציגה הודעת Success.
אין מיגרציה חדשה בשלב התנועות והתאריכון. אין להריץ מחדש את סכמת ההקמה בלי לבדוק את המסד הקיים.

## הרצה ופרסום
npm ci
node scripts/test-transactions.cjs
npm run build
npm run dev

GitHub: https://github.com/MelodyBer/invoice-manager
אתר: https://invoice-manager-nu-eight.vercel.app
פרסום נעשה דרך Vercel CLI: npx --yes vercel deploy --prod --yes.
אין להניח שקישור אוטומטי GitHub→Vercel פעיל. יש לאמת תוצאת פרסום READY והכתובת הראשית אחרי כל פרסום.
המשתמשת אישרה פרסום של שלב התנועות והתאריכון ושמירת כל הקוד ב־GitHub לצורך המשך עם מודל אחר.

## הבדיקות הבאות
סינון תאריכים/קטגוריה/סוג/חיפוש; מיון כל עמודה; עימוד עם סיכומים קבועים; פתיחת מסמך, עריכה ורענון; ביטול מחיקה ומחיקת נתון בדיקה בלבד; ניווט תאריכון ותאריך מוכן בהזנה ידנית; תצוגת מובייל; בידוד בין שני משתמשים.


עדכון: אפשרות ללא מע״מ ושיפור תגובה
- בטופס המשותף נוספה בחירת ללא מע״מ, שמאפסת שיעור וסכום מע״מ והכרה, תוך שמירת הסכום הכולל. ביטול הבחירה חוזר ל־18%; ניתן לערוך את השיעור.
- חילוץ מקבל הנחיה מפורשת למסמך עוסק פטור; תיקון סכומים מכבד שיעור אפס.
- אימות המשתמש משותף בין מעטפת ודף באותה בקשת שרת בלבד באמצעות React cache; אין מטמון משותף למשתמשים. אימות middleware ו־RLS נשארים פעילים.
- בתאריכון קריאות בלתי תלויות נשלחות במקביל ונוסף מסך טעינה למעטפת.
- בדיקות הטופס לחישוב ללא מע״מ ובניית הייצור עברו. טרם נמדדה מהירות מתוך חשבון מחובר; אין טענה שהאיטיות נפתרה במלואה.
- קבצים: src/components/transactions/TransactionForm.tsx, src/lib/transactions/use-transaction-form.ts, src/lib/transactions/build-initial-values.ts, src/lib/extraction/schema.ts, src/lib/extraction/validate-result.ts, src/lib/transactions/load-range.ts, src/app/(app)/layout.tsx, src/app/(app)/calendar/page.tsx, src/app/(app)/loading.tsx, scripts/test-transactions.cjs, PROJECT_STATUS.md.
- בדיקה ידנית: אישור מסמך ללא מע״מ בסך 250 ₪, שינוי הסכום והקטגוריה, שמירה ופתיחה מחדש; בדיקת מסמך רגיל; ניווט תנועות ותאריכון בטלפון.


עדכון המשך ביצועים: המשתמשת דיווחה על איטיות בניווט, בזיהוי ובפתיחה ושמירה. מדידה בודדת לדף login הראתה כ־0.9 שניות לתגובה ראשונה; אין מדידת חשבון מחובר. קריאות קטגוריות ומסמך בפרטי תנועה ובבדיקות לפני יצירה מופעלות במקביל. נוספו זמני queue/download/recognition/save ביומן השרת תחת extraction_timing בלבד, בלי נתוני מסמכים, מזהי משתמשים או סודות. נוספו מסנני user_id לעדכוני סטטוס ותוצאות חילוץ. עדיין יש למדוד זיהוי אמיתי לפני קביעה שהוא הואץ. קבצים: src/lib/transactions/detail-actions.ts, src/lib/transactions/save-transaction.ts, src/lib/extraction/extract-document.ts, src/app/api/documents/[id]/extract/route.ts, PROJECT_STATUS.md.


עדכון מסך אישורים (15/09/2026)
- documents מציג רק מסמכים ללא תנועה מאושרת. helper משותף לרשימה ולתור האישור, עם עימוד פנימי ובדיקת קישורים רק למסמכים שבאצווה; כל קריאה מסוננת לפי user_id.
- פתיחת קישור אישור למסמך מאושר מובילה לתנועה עם כפתור עריכה. לא מוחקים מסמכים מאושרים, ולא יוצרים אותם מחדש דרך מסך האישור.
- נוסף קישור מסמכים לאישור בתפריט, מצבי רשימה ברורים, כותרות בטופס, טופס כלשונית ראשונה בנייד וסרגל אישור דביק.
- חילוץ חדש משתמש בסוג התנועה: בהכנסה הלקוח, בהוצאה הספק. שם לא ברור מוחזר ריק בציון ביטחון אפס. נתונים שכבר חולצו או אושרו לא שונו אוטומטית.
- בדיקות אוטומטיות: סינון מאושרים, פילטרי בעלות, יותר מ־200 מסמכים, שגיאת שאילתה, והוראות שונות להכנסה ולהוצאה. לא נבדק חילוץ חי עם מסמך פרטי או ממשק בחשבון מחובר.
- קבצים: src/lib/transactions/review-queue.ts (חדש), src/components/transactions/EditTransactionButton.tsx (חדש), src/app/(app)/documents/page.tsx, src/app/(app)/documents/[id]/review/page.tsx, src/app/(app)/transactions/[id]/page.tsx, src/components/transactions/TransactionForm.tsx, src/components/layout/nav-links.ts, src/lib/transactions/detail-actions.ts, src/lib/extraction/schema.ts, src/lib/extraction/extract-document.ts, scripts/test-transactions.cjs, PROJECT_STATUS.md.
- פקודות: node scripts/test-transactions.cjs; npm run build. אין SQL או ספריות נוספות.
- בדיקות ידניות: אישור מסמך והיעלמותו מהרשימה גם אחרי רענון; עריכת התנועה; פתיחת קישור אישור ישן; אישור ועבור לבא; רשימה ריקה; מסמך הכנסה עם מנפיק ולקוח שונים לעומת מסמך הוצאה; לשוניות וסרגל אישור בטלפון.

עדכון חיבור חשבונית וקבלה והסרה מתור האישורים
- פורסם: הענף document-pairing-review מוזג ל-main, והקוד 0b5d851 פורסם ב-Vercel במצב READY. נבדקו קיום transaction_id ו-dismissed_at וקיום שתי פונקציות ה-RPC במסד החי; גישה אנונימית לפונקציות נחסמת. בדיקת תהליך מלא בחשבון מחובר עדיין נדרשת.
- שני שדות חדשים ב-documents: transaction_id (קישור בבעלות אותו משתמש באמצעות FK מורכב), dismissed_at (הסרה מהתור ללא מחיקת הקובץ).
- פונקציות SQL security invoker תחת RLS: confirm_documents שומרת קישור ותנועה באותה טרנזקציה, עם נעילת מסמכים וסינון auth.uid; dismiss_review_document מסירה מהתור אחרי בדיקת קישור לתנועה.
- זיהוי הצעות מבוסס צד זהה (מספר עסק אם לשניהם יש, אחרת שם מנורמל), כיוון זהה, invoice_tax לעומת receipt, סכום זהה עד אגורה, שקלים בלבד, קבלה מתאריך החשבונית עד 90 יום אחריה. אין חיבור אוטומטי ללא בחירת המשתמשת. סכום חלקי, מטבע זר, תאריך חסר או שני מסמכים מסוג חשבונית מס-קבלה לא מחוברים.
- זוג יחיד ולא עמום מוצג יחד ברשימת האישורים. במסך האישור מוצגים המסמך הראשי והצעות עם תצוגת המקור. כשיש הצעה צריך לבחור חיבור או עסקה נפרדת במפורש.
- אם החשבונית כבר אושרה, צירוף הקבלה משאיר את נתוני התנועה הקיימת. אם הקבלה אושרה קודם, האישור המשותף מעדכן את אותה תנועה לפרטי החשבונית ומציג על כך הודעה. שתי תנועות קיימות נפרדות אינן ממוזגות אוטומטית.
- כל אישור חדש של מסמך משתמש ב-RPC אטומי; הזנה ידנית ללא מסמך נשארת במסלול הקיים.
- חלונית התנועה מציגה את כל המצורפים. מחיקת תנועה מבקשת אישור למחיקת כל המסמכים. מחיקת Storage ומסד הנתונים עדיין אינה אטומית; קיימת הודעת ניסיון חוזר.
- הסרה מהתור היא הסתרה בלבד, לא מחיקה לצמיתות; אין בשלב זה מסך שחזור להסתרות.
- בדיקות TypeScript, build וסקריפט הבדיקות עברו. בדיקות SQL בפועל, RLS על שני חשבונות והתהליך בדפדפן מחובר עדיין לא בוצעו; אין סביבת PostgreSQL מקומית. אין ספריות חדשות.
- עדכון migration_3_document_pairing.sql זמין במסד, והמיזוג והפרסום הושלמו. אין צורך להריץ שוב את הסכמה הראשונית.
- קבצים חדשים: supabase/migration_3_document_pairing.sql, src/lib/transactions/document-matching.ts, src/lib/transactions/pair-actions.ts, src/lib/transactions/transaction-payload.ts, src/components/documents/RemoveReviewDocument.tsx.
- קבצים ששונו: src/types/db.ts, src/lib/transactions/save-transaction.ts, src/lib/transactions/review-queue.ts, src/lib/transactions/detail-actions.ts, src/components/transactions/TransactionDrawer.tsx, src/app/(app)/documents/page.tsx, src/app/(app)/documents/[id]/review/page.tsx, scripts/test-transactions.cjs, PROJECT_STATUS.md.
- פקודות: node scripts/test-transactions.cjs; npm run build.
- בדיקות ידניות אחרי פרסום: 1. חשבונית 118 וקבלה 118 מאותו צד — אישור משותף, תנועה אחת בסך 118 ושני מצורפים; שניהם יוצאים מהתור. 2. קבלה שמגיעה אחרי אישור החשבונית — צירוף ללא שינוי סכומים או ספירה נוספת. 3. חשבונית לאחר אישור קבלה — בדיקה ואישור עדכון התנועה הקיימת. 4. צד או מטבע אחר/סכום חלקי — אין הצעת חיבור. 5. ביטול חיבור ועסקה נפרדת במפורש. 6. ביטול הסרה לעומת אישור הסרה ורענון; הקובץ נשמר. 7. ניסיון מקביל בשתי לשוניות או שינוי תנועה לפני אישור — אין תנועה כפולה. 8. חשבון שני לא רואה ולא מחבר מסמכים של הראשון. 9. כל התהליך בטלפון.

תיקון קבלה מול חשבונית מאושרת: בדיקת התאמה משתמשת בשדות התנועה שאושרו במקום נתוני חילוץ ישנים. נוסף חיפוש חשבוניות מאושרות לפי תאריכי התנועה; התרעת כפילות נבדקת להצעת צירוף לפני הצגת אפשרות שמירה נוספת. חיבור דורש אישור חוזר ושומר את סכומי החשבונית. שגיאת התאמה אינה עוקפת כפילות. אין SQL חדש. קבצים: src/lib/transactions/document-matching.ts, src/lib/transactions/pair-actions.ts, src/app/(app)/documents/[id]/review/page.tsx, scripts/test-transactions.cjs, PROJECT_STATUS.md. בדיקה ידנית: קבלה מול חשבונית שנערכה באישור, אישור הצירוף, תנועה אחת ושני קבצים.

## עדכון מטבע וצירוף קבלה — פורסם ב־16/09/2026
- המשתמשת אישרה שהריצה את migration_4. בדיקה ללא קריאת נתונים פרטיים אישרה שהעמודות זמינות בשתי הטבלאות ושפונקציית save_financial_record קיימת וחסומה לאנונימיים. הענף currency-and-receipt-linking מוזג ל-main ונדחף ל-GitHub. קוד 5dde41a פורסם ב-Vercel במצב READY והכתובת הראשית עודכנה. בדיקה מלאה עם משתמש מחובר עדיין נדרשת.
- מטבע מקורי הוא ILS או USD. סכומי transactions.amount_before_vat / vat_amount / amount_total נשארים בשקלים; original_amount_* שומרים את מטבע המקור. amount_total_usd שומר את הערך בדולרים גם למסמך שקלי. כל הסכומים numeric(12,2), השער numeric(18,8).
- exchange_rate הוא תמיד מספר השקלים לדולר אמריקאי, גם עבור מסמך שמטבעו שקל. exchange_rate_date הוא יום פרסום השער בפועל. documents.valuation_date שומר את תאריך המסמך שהניע את ההמרה. documents.verified_doc_type ו-verified_counterparty_name שומרים נתונים שאושרו בלי לשכתב extraction_raw.
- מקור השערים: https://www.boi.org.il/information/bank-paymnts/guide/api-guide/ ; סדרה RER_USD_ILS בממשק SDMX הרשמי. נבדקה בקשת רשת אמיתית לטווח 01–08/02/2026, והשער ל־06/02/2026 היה 3.125. קובץ CSV נפרס ללא ספרייה נוספת. בוחרים רק שער מתאריך המסמך או מוקדם ממנו, לא שער עתידי. תאריך עתידי או לפני 2020 נדחה.
- getBoiRate בשרת בלבד; מתאריך תחילה של 30 יום אחורה ומרחיב חיפוש אם אין נתונים. מטמון fetch ציבורי לפי כתובת ותאריך (ללא מידע משתמשים). בשגיאת רשת אין שער משוער ואין שמירה. הבנק מקבל רק טווח תאריכים, לא מסמכים ולא שמות.
- החישוב משתמש באגורות וב-BigInt כדי לעגל המרות. סכום כולל ולפני מע"מ מומרים ומע"מ בשקלים הוא ההפרש כדי לשמור על איזון. הסכומים המקוריים נשמרים בנפרד.
- RPC חדש save_financial_record עם SECURITY INVOKER, RLS ו-auth.uid, נעילת מסמכים, בדיקת בעלות וגרסת updated_at. חיבור מסמך ויצירה/עדכון של תנועה מתבצעים באותה טרנזקציה. פקודות העדכון הישנות נשמרות למסד הקיים אך קוד האפליקציה עבר למסלול החדש. אין שינוי מפתחות או Service Role.
- ReceiptAttachment מציג חיפוש של עד 20 חשבוניות מאושרות לפי ספק או מספר; לכל אחת כפתור 'צרף קבלה לחשבונית' שפותח דיאלוג עם פרטי שני המסמכים והמרותיהם. 'אשר צירוף' מבצע בפועל את החיבור. ניתן לאשר במפורש גם מסמכים במטבעות או סכומים שונים, ללא ניחוש אוטומטי שהם שייכים לאותה עסקה. צירוף לחשבונית קיימת אינו משנה את סכומי החשבונית. קבלה שאושרה קודם ואחריה חשבונית עדיין ניתנות לאישור משותף דרך תור ההתאמות.
- רשומות ישנות שמסומנות USD או שהמסמך שלהן זוהה כמטבע אחר, ונשמרו ללא שער, מסומנות currency_review_required. המספרים הישנים לא מומרים אוטומטית. הרשומות נשארות גלויות לעריכה אך אינן נכללות בסיכומים עד לשמירה מחדש. בעריכה שומרים שם, מספר, קטגוריה והערות שתוקנו קודם, וטוענים את הסכומים המקוריים מהחילוץ לבדיקה. למסמכי שקל ישנים ללא שער מופיעה הודעה שהערך בדולר טרם חושב.
- חילוץ מודע לדולר ולשקל. לא ממיר מטבע במודל; אינו מניח 18% במסמך דולרי ללא פירוט מס, ומוסיף הערה לבדיקת המס. מטבעות אחרים אינם נשמרים עד בחירת מטבע נתמך.
- אין ספריות נוספות ואין שינוי בהעלאת הקבצים.
- אימות שבוצע: node scripts/test-currency.cjs ; node scripts/test-transactions.cjs ; npm run build — עברו. בדיקות כוללות סוף שבוע ללא שער, שורות CSV עתידיות, USD/ILS, עיגול, מע"מ אפס, תאריכים שונים לכל מסמך, צירוף בלי עדכון סכום החשבונית, סינון בעלות, שגיאת שער ושגיאת גרסה. בדיקות בסיס נתונים מלאות תחת RLS, ה-RPC החדש על PostgreSQL ותהליך דפדפן מחובר טרם בוצעו; אין סביבת PostgreSQL מקומית.
- ההתקנה, בדיקת זמינות הסכמה, המיזוג והפרסום הושלמו. אין צורך להריץ SQL נוסף. בדיקות ידניות בחשבונות מחוברים ובטלפון נשארו לביצוע כמפורט להלן.
- בדיקות ידניות: (1) מסמך 100 דולר ללא מע"מ, 08/02/2026 — שער 06/02/2026, 3.125, סה"כ 312.50 ₪ ו־100 $. (2) מסמך 312.50 ₪ באותו תאריך — נשאר 312.50 ₪ ומוצג גם 100 $. (3) חשבונית וקבלה בתאריכים/מטבעות שונים — בדיקת שני שערים, אישור צירוף ותנועה אחת ללא שינוי הסכום המקורי. (4) כשל שירות שערים — השמירה נעצרת, אין תנועה חלקית. (5) עריכת תאריך או מטבע — חישוב מחדש ושמירה; פתיחה מחדש מציגה אותו שער שנשמר. (6) תיקון רשומה ישנה שסומנה לבדיקה — חוזרת לסיכומים רק לאחר שמירה. (7) חשבון אחר לא יכול לצפות או לצרף מסמך שאינו שלו. (8) חיפוש, פתיחת דיאלוג, ביטול ואישור צירוף בנייד.

קבצים שנוצרו או שונו בשלב זה:
- PROJECT_STATUS.md
- scripts/test-currency.cjs
- scripts/test-transactions.cjs
- src/app/(app)/calendar/page.tsx
- src/app/(app)/documents/[id]/review/page.tsx
- src/app/(app)/transactions/[id]/page.tsx
- src/app/(app)/transactions/page.tsx
- src/components/documents/ReceiptAttachment.tsx
- src/components/transactions/CurrencyPreview.tsx
- src/components/transactions/MoneySummary.tsx
- src/components/transactions/TransactionDrawer.tsx
- src/components/transactions/TransactionForm.tsx
- src/components/transactions/TransactionResults.tsx
- src/lib/currency/actions.ts
- src/lib/currency/boi.ts
- src/lib/currency/money.ts
- src/lib/extraction/parse-result.ts
- src/lib/extraction/schema.ts
- src/lib/transactions/attach-receipt.ts
- src/lib/transactions/build-initial-values.ts
- src/lib/transactions/detail-actions.ts
- src/lib/transactions/document-matching.ts
- src/lib/transactions/financial-actions.ts
- src/lib/transactions/financial-server.ts
- src/lib/transactions/form-from-transaction.ts
- src/lib/transactions/pair-actions.ts
- src/lib/transactions/reporting.ts
- src/lib/transactions/save-transaction.ts
- src/lib/transactions/validate-values.ts
- src/types/db.ts
- src/types/transaction-form.ts
- supabase/migration_4_currency.sql


## פישוט בחירת חיבור במסך האישור — 21/09/2026
- הצעת ההתאמה האוטומטית מופיעה ראשונה באזור חיבור אחד. חיפוש חשבוניות מאושרות סגור כברירת מחדל, נפתח רק ביוזמת המשתמשת, ומסומן כרשימה כללית ולא כהתאמות. אין קריאת חיפוש נוספת עד לפתיחה.
- בחירת התאמה סוגרת את החיפוש ומסבירה שיש להשלים באישור החיבור בתחתית. במקרה של כפילות ללא התאמה החיפוש נפתח אוטומטית. אין שינוי בשמירה או בסכומים.
- קבצים: src/app/(app)/documents/[id]/review/page.tsx, src/components/documents/ReceiptAttachment.tsx, PROJECT_STATUS.md.
- בדיקות שעברו: node scripts/test-transactions.cjs; node scripts/test-currency.cjs; npm run build. לא בוצעה בדיקת ממשק בחשבון מחובר.
- בדיקה ידנית: הצעה מופיעה ללא רשימה כללית; פתיחת וסגירת חיפוש אחר; בחירת התאמה וביטולה; השלמת חיבור ותנועה אחת עם שני מסמכים; אותו תהליך בטלפון. אין SQL או התקנות נוספות.


## זיהוי מוקדם של כפילות והשוואה בצד — 21/09/2026
- הבדיקה מתבצעת בפתיחת מסמך ובשינוי שדות, עם השהיה של 350ms וביטול תשובות ישנות. נבדקים שם, מספר, תאריך, סוג מסמך וכיוון מול תנועות מאושרות של המשתמש בלבד. סוגים שונים כגון חשבונית וקבלה לא מוצגים כעותק זהה. בדיקת הכפילות בעת שמירה נשארת.
- התראה מציעה השוואה או הסרה מתור האישור, עם אישור ההסרה הקיים. אין מחיקת תנועה או קבצים. ההשוואה מציגה פרטים ומצורפים מאושרים משמאל והטופס נשאר מימין. בטלפון התצוגה מתחת לטופס ללא כיסוי. אפשר לפתוח גם את המקור הממתין. תנועה ללא מצורף מציגה פרטים בלבד.
- קבצים: src/components/documents/ReviewComparison.tsx (חדש), src/components/documents/RemoveReviewDocument.tsx, src/app/(app)/documents/[id]/review/page.tsx, src/lib/transactions/duplicate-check.ts, scripts/test-transactions.cjs, PROJECT_STATUS.md.
- בדיקות שעברו: node scripts/test-transactions.cjs; npm run build. נוספו בדיקות סינון בעלות/מאושרים/סוג מסמך, שדות חסרים, מספר ריק וכשל שאילתה. בדיקת דפדפן בחשבון מחובר טרם בוצעה.
- בדיקות ידניות: התראה לפני שמירה; פתיחה וסגירת השוואה בלי הסתרת הטופס; שינוי שדות מבטל התאמה ישנה; ביטול הסרה ואז הסרה תוך שמירת התנועה המקורית; ללא מצורף; חשבון אחר; מובייל. אין SQL או התקנות חדשות.


## שיפור פתיחת מסמך לאישור — 21/09/2026
- הטופס מוצג לאחר טעינת המסמך, הקטגוריות, הפרופיל ובדיקת קישור קיים. תור האישורים וההתאמות נטענים בנפרד לאחר מכן ואינם חוסמים הצגת הטופס. האישור חסום עד השלמת בדיקת התאמות; אישור ועבור לבא זמין לאחר טעינת התור. כשל בתור לא חוסם שמירה רגילה.
- שאילתת תנועה נפרדת לכל מסמך הוחלפה בשאילתות אצווה לפי מזהים, עם סינון user_id ועימוד לקישורים ישנים. בדיקה של 30 מסמכים מאמתת 3 קריאות כולל מקור ואצוות מסמכים במקום 32. אין שינוי בכללי התאמה.
- עברו test-transactions.cjs, test-currency.cjs ו-npm run build. כלי Chrome DevTools אינו זמין; לא נמדד זמן פתיחה בחשבון מחובר ואין טענה לכמות שניות שנחסכה.
- קבצים: src/app/(app)/documents/[id]/review/page.tsx, src/lib/transactions/pair-actions.ts, scripts/test-transactions.cjs, PROJECT_STATUS.md.
- בדיקה ידנית: פתיחת מסמך מרשימת האישורים; הופעת פרטים לפני סיום ההתאמות; שמירה רק בסיום הבדיקה; שמירת ההצעות והתרעות הכפילות; מעבר לבא וניווט מהיר בין מסמכים; נייד. אין SQL או פקודות נוספות למשתמשת.


## תיקון כרטיסי מסמכים בנייד — 21/09/2026
- רשימת האישורים משתמשת בפריסה אנכית בנייד: שם ופרטים ברוחב הכרטיס, פעולות מתחתיהם עם גלישה לשורה נוספת. שמות הקובץ הראשי והקבלה המקושרת מקוצרים לשורה אחת עם ellipsis וכיוון טקסט אוטומטי. במחשב נשמרת פריסה אופקית. גם סיכום התור מסתדר אנכית בנייד.
- קבצים: src/app/(app)/documents/page.tsx, PROJECT_STATUS.md. npm run build עבר. אין שינויים בנתונים, ספריות או SQL. לא בוצעה בדיקה באייפון בפועל.
- בדיקות ידניות: שם קובץ ארוך וכרטיס עם קבלה מקושרת ברוחב טלפון; אין אותיות בטור או גלילה אופקית; כפתורי אישור והסרה נגישים; תצוגת מחשב. אין פקודות להרצה.


## הסתרת המרה בטופס שקלי — 21/09/2026
- CurrencyPreview מציג המרה וטוען שער לתצוגה רק כאשר המטבע USD. בשקל או מטבע אחר הרכיב אינו מוצג. החלפת מטבע מבטלת תוצאות בקשה קודמת. שמירה וחישובים בשרת ללא שינוי.
- קבצים: src/components/transactions/CurrencyPreview.tsx, PROJECT_STATUS.md. npm run build עבר. בדיקה ידנית: טופס שקלי ללא המרה; דולרי עם שער וסכום בשקלים; מעבר בין שני המטבעות. אין SQL או פקודות להרצה.


## שמירה שקלית ללא פנייה לבנק ישראל — ממתין ל־migration_5
- prepareFinancialValues משתמש ב־shekelMoney עבור ILS בלבד: הסכומים נשארים בשקלים; exchange_rate, exchange_rate_date, amount_total_usd הם null. מסלול USD נשאר getBoiRate ו-convertMoney ללא שינוי. מגבלות תאריך 2020 עד היום נשמרות גם בשקל. התאמת אגורה במע״מ נשארת כפי שהייתה.
- MonetaryValues מאפשר null; ConvertedMonetaryValues שומר טיפוסים מספריים למסלול עם שער. MoneySummary אינו מציג אזהרת שער חסר למסמך שקלי ללא שער. אין שינוי בהתאמות, כפילויות, חילוץ, RLS או סינון בעלות.
- migration_5_ils_without_exchange_rate.sql מחליף רק valid_currency_values: ILS בלי שער מותר, USD עדיין דורש שער, ונתיב ILS ישן עם שער נשאר תקין. העמודות כבר nullable ב-migration_4. אין עדכון רשומות. בדיקות SQL מובנות רצות באותה טרנזקציה לפני commit; לא הורצו על PostgreSQL בסביבה זו.
- חובה להריץ migration_5 לפני פרסום האפליקציה. אין לפרסם את הקוד עד התקנת SQL. הגרסה הקודמת יכולה לעבוד עם האימות החדש.
- בדיקות שעברו: node scripts/test-transactions.cjs; node scripts/test-currency.cjs; npm run build. בדיקות חדשות: אפס קריאות שער ב-ILS בשמירה רגילה, ידנית וצירוף; שדות null; סכומים ותאריכים לא תקינים נדחים; USD נכשל כשאין שער וממשיך להמיר עם שער אמיתי.
- מדידת משתמשת לפני התיקון באתר החי עם משתמשת מחוברת: כ־3.5 שניות מלחיצה על אשר ושמור. אין מדידה חיה אחרי התיקון עדיין. לאחר פרסום למדוד 3–5 שמירות של מסמכי ILS חדשים דומים באותו מכשיר/רשת, מהלחיצה עד הודעת הצלחה/מעבר; לרשום כל זמן וחציון. לבדוק 118 ₪ (100+18), ללא מע״מ, USD ושמירת שערים ישנים ללא עריכה; לבדוק שבקשת BOI אינה מתרחשת בנתיב ILS.
- קבצים: src/lib/transactions/financial-server.ts, src/lib/currency/money.ts, src/components/transactions/MoneySummary.tsx, scripts/test-currency.cjs, supabase/migration_5_ils_without_exchange_rate.sql (חדש), PROJECT_STATUS.md.


עדכון פרסום migration_5: המשתמשת דיווחה Success; בדיקת RPC עם ערכי דוגמה בלבד במסד החי אישרה ILS ללא שער=true ו-USD ללא שער=false. הענף מוזג ל-main וקוד 51769f2 פורסם ב-Vercel במצב READY. הוראת ההמתנה לפרסום לעיל הושלמה. עדיין נדרשת מדידת אשר ושמור עם משתמשת מחוברת לאחר הפרסום; אין תוצאת מדידה חדשה.

## מיפוי בדיקות במסך האישור — 22/09/2026 (חלק ג׳, תיעוד בלבד)

כפילות היא חשד שאותו מסמך הוזן פעמיים. התאמה היא חשד לשני מסמכים שונים — חשבונית מס וקבלה — של אותה עסקה. התאמה אינה הוכחה לכפילות; לא מחברים או מסירים אוטומטית.

| בדיקה | מתי | מה נבדק בפועל | תוצאה למשתמשת |
| --- | --- | --- | --- |
| findDocumentPairs / pair-actions.ts | ברקע לאחר טעינת פרטי המסמך | אותו user_id וכיוון; זוג invoice_tax/receipt; שם מנורמל או מספר עסק כשקיים בשניהם; סכום מקורי זהה עד אגורה ובאותו מטבע ILS/USD; קבלה בתאריך החשבונית או עד 90 יום אחריה. כולל מסמכים ממתינים וחשבוניות מאושרות; משתמש בנתוני התנועה המאושרים כשיש קישור ראשי | מציג הצעה לחיבור או החלטה על עסקה נפרדת; אינו יוצר חיבור בלי אישור |
| findApprovedDuplicateId בתוך ReviewComparison.tsx | בפתיחת המסך ובשינוי שם, מספר, תאריך, סוג או כיוון, אחרי 350ms; מושבת בעת חיבור נבחר או שמירה | תנועה מאושרת בלבד, אותו user_id, שם מדויק לאחר trim של הקלט, מספר מדויק או null, תאריך, סוג מסמך וכיוון זהים. אין בדיקת סכום/מטבע/תוכן קובץ. לכן זו התאמה בפרטים, לא הוכחה שזה אותו קובץ | התרעה לפני שמירה, השוואה צד לצד, הסרה מאישור לאחר החלטה מפורשת; אינו חוסם שמירה בעצמו |
| findDuplicateTransactionId / duplicate-check.ts | בלחיצה על שמירה אחרי אימות הטופס | אותו user_id, שם, מספר או null ותאריך. בניגוד לבדיקה המוקדמת אינו מסנן is_verified, doc_type או direction, ואינו משווה סכום או מטבע | נבדקת תחילה אפשרות resolveInvoiceDuplicate לצירוף קבלה לחשבונית; אחרת דיאלוג כפילות עם קישור, ביטול או שמירה בכל זאת |

פערים קיימים: התאמה נטענת לפי נתוני החילוץ המקוריים ואינה רצה מחדש בכל תיקון בטופס; הבדיקה המוקדמת והבדיקה בשמירה פועלות על הטופס אך עם מסננים שונים. קבלה וחשבונית עם אותו מספר/שם/תאריך עשויות להפעיל בדיקת שמירה אף שאינן עותקים. תנועת יעד נבחרת לחיבור אינה נחשבת כפילות שחוסמת את אותו חיבור. בדיקות אלו אינן נעילה אטומית; RPC השמירה בודק בנפרד קישור מסמך קיים ובעלות. קוד הבדיקות וכללי ההתאמה לא שונו כחלק ג׳.

בדיקת התיעוד: הושווה ל-review/page.tsx, ReviewComparison.tsx, duplicate-check.ts, pair-actions.ts ו-document-matching.ts. רק PROJECT_STATUS.md השתנה בקומיט ג׳.

### חלק א׳ — מדידת שרת
financial-actions.ts רושם שורה אחת save_timing לכל הפעלה של saveFinancialTransaction, גם בשגיאה, עם stage=total, durationMs ו-timings של auth, prepare, rpc, revalidate (רק שלבים שהתחילו). אין ערכים, מזהים, מטבע, תוכן או הודעות שגיאה בלוג. סך הזמן נמדד מכניסה לפונקציה ועד finally לפני ההחזרה: אינו כולל cold-start לפני כניסה לפונקציה, בדיקות כפילות בדפדפן, תעבורת רשת או ניווט. זה אינו נתיב approveDocumentPair או attachReceipt. יש לשמור מסמך רגיל דרך אשר ושמור כדי למדוד את הנתיב הזה. בדיקות currency והטיפוסים עברו; הזמנים מסקריפט הבדיקות הם של mocks ואינם מדידות אמיתיות.

### חלק ב׳ — זמינות כפתורים
אין נעילה לפי isCheckingPairs כשאין הצעה שמצריכה החלטה. כשיש pairs ללא בחירת pair וללא separateConfirmed, שני כפתורי השמירה חסומים. שגיאת התאמה ממשיכה לחסום. לפני השמירה עצמה נבדקת החלטה עדכנית גם אם הצעה הגיעה בזמן בקשת הכפילות. הצעה שמגיעה אחרי תחילת פעולת השמירה אינה מבטלת בדיעבד את הבקשה. אשר ועבור לבא אינו מחכה ל-queueReady לפני השמירה; במידת הצורך טוען תור אחרי השמירה, ובכשל חוזר לרשימת המסמכים בלי לבטל שמירה שהצליחה. בדיקות transactions והטיפוסים עברו.

קבצי שלב זה: src/lib/transactions/financial-actions.ts (א׳), src/app/(app)/documents/[id]/review/page.tsx (ב׳), PROJECT_STATUS.md (ג׳). אין SQL, ספריות או שינוי RLS.
פקודות: node scripts/test-currency.cjs; node scripts/test-transactions.cjs; npm run build.
בדיקות ידניות: בלי pairs כפתורים פעילים בזמן בדיקת רקע; עם pairs נדרשת החלטה; הצעה שמגיעה בזמן בדיקת כפילות עוצרת לפני שמירה; אשר ועבור לבא לפני טעינת התור; כשל טעינת תור אחרי שמירה; שמירת מסמך ILS ואחריו USD בנפרד ואיסוף שורות save_timing מהיומן בפועל. מדידות חיות עדיין ממתינות לשמירות לאחר הפרסום.


עדכון פרסום 22/09/2026: חלק א׳ 79f761b; חלק ב׳ 5bf9c96 ותיקון המשך 86c3268 למניעת לחיצה חוזרת בזמן טעינת הבא; חלק ג׳ 16380f0 (תיעוד בלבד). פורסם ב-READY, וכל הקומיטים ב-main ב-GitHub. גם בדיקה ממוקדת של instrumentation עברה: הצלחה, כשל auth/prepare/RPC, שורה אחת לכל בקשה, גבולות שלבים מדויקים וללא נתונים פרטיים. בדיקה זו השתמשה בשעון ובפעולות מדומים ואינה מדידה חיה. קריאת vercel logs --environment production --query save_timing --since 15m הצליחה אך לא החזירה מדידות עדיין. נשלחה למשתמשת בקשה לשמור ILS ואז USD ולמסור שעה והצלחה; ממתינים לכך כדי להשלים את חלק א׳. אין כרגע מדידות אמיתיות שאפשר לדווח.

## חיבור שתי תנועות מאושרות — ממתין ל-migration_6
- MergeApprovedDocuments מופיע בפרטי תנועה ובחלונית המשותפת לתנועות/תאריכון, כאשר זו חשבונית מס או קבלה מאושרת עם מסמך. חיפוש ידני משני הכיוונים לפי שם/מספר, מסונן user_id, סוג משלים, כיוון זהה, is_verified ומטבע שאינו טעון בדיקה. לא שונה מסך האישור של מסמכים ממתינים.
- לפני החיבור נטענים שני המסמכים עם הפרטים העדכניים. המשתמשת מאשרת במפורש שתישאר תנועה אחת לפי החשבונית ותנועת הקבלה תוסר מהסיכומים. אין ניחוש אוטומטי או חיבור שקט; מטבעות וסכומים שונים מותרים רק אחרי ההשוואה והאישור. אין ביטול אוטומטי.
- migration_6_merge_approved_documents.sql יוצר RPC SECURITY INVOKER תחת RLS ו-auth.uid. נועל מסמכים ואז תנועות, בודק שתי גרסאות updated_at, סוגים משלימים, כיוון, שתי תנועות מאושרות עם מסמך אחד כל אחת, בעלות, קשרים זרים/משותפים ומטבעות שסומנו לבדיקה. קבוצות שכבר מכילות כמה מסמכים חסומות בשלב זה.
- שתי הרשומות documents מקושרות לחשבונית, קבצי Storage אינם נמחקים; סכומי/שערי החשבונית אינם משתנים. מסמכים ישנים מקבלים תמונת סכומים מתנועתם המאושרת רק אם valuation_date חסר. פרטי הקבלה המאושרת במלואם נשמרים במפתח merged_approved_transaction בתוך extraction_raw בלי לדרוס שדות החילוץ המקוריים; הערותיה מצורפות להערות החשבונית. תנועת הקבלה הנפרדת נמחקת באותה טרנזקציה. אין fetch לשער ואין המרת נתונים קיימים.
- נדרש להריץ migration_6 פעם אחת אחרי migration_5 לפני פרסום. אין שינוי בטבלאות או בסכמת ההקמה. אין ספריות חדשות.
- קבצים חדשים: src/components/transactions/MergeApprovedDocuments.tsx, src/lib/transactions/merge-approved.ts, supabase/migration_6_merge_approved_documents.sql, scripts/test-merge-approved.cjs.
- קבצים ששונו: src/components/transactions/TransactionDrawer.tsx, src/app/(app)/transactions/[id]/page.tsx, src/types/db.ts, PROJECT_STATUS.md.
- עברו: node scripts/test-merge-approved.cjs; node scripts/test-transactions.cjs; node scripts/test-currency.cjs; npm run build. בדיקות חדשות מכסות חיפוש דו-כיווני, מסנני בעלות וכיוון, אישור, מקור חסר, העברת שתי גרסאות ותגובת שגיאה. RPC לא הורץ על PostgreSQL ולא בוצעה בדיקת חשבון מחובר; אין לטעון שנבדק מיזוג חי.
- בדיקות ידניות לאחר התקנה ופרסום: חשבונית→קבלה וגם קבלה→חשבונית; ביטול לפני חיבור; תנועה אחת עם שני קבצים וסכומי החשבונית בלבד; הסרת ספירה כפולה בתנועות ובתאריכון; USD/ILS בתאריכים שונים שומרים סכומים ושערים; הערות/מספר קבלה שתוקן נשמרים; תנועה ששונתה בלשונית אחרת נדחית; קבוצה קיימת נדחית; חשבון אחר אינו יכול לחבר נתונים; נייד.
- מדידות save_timing מהשלב הקודם עדיין ממתינות לשמירות רגילות של המשתמשת. פעולת המיזוג החדשה אינה עוברת דרך saveFinancialTransaction.


עדכון פרסום migration_6: המשתמשת דיווחה שבוצע. בדיקה עם פרמטרים ריקים וללא התחברות החזירה 401/42501, המעידה שהפונקציה קיימת והרצתה לאנונימיים חסומה; לא שונו נתונים. הענף מוזג ל-main, וקוד a48aaa3 פורסם ב-Vercel במצב READY. אין צורך ב-SQL נוסף. חיבור אמיתי של שני מסמכים תחת חשבון מחובר עדיין נדרש לבדיקת המשתמשת.


## תיקון גלישת PDF בהשוואה במחשב
- DocumentViewer: הוחלף h-full/min-height בגובה עצמאי clamp(320px,65vh,600px), iframe מסוג block בתוך מסגרת overflow-hidden. מונע תלות מחזורית בגובה כרטיס המכיל גם פרטים וכפתורים.
- MergeApprovedDocuments: עמודות לפי רוחב זמין בפועל באמצעות auto-fit/minmax, במקום xl לפי רוחב חלון גם בחלונית צרה. אין שינוי בשמירה או בנתונים.
- קבצים: src/components/documents/DocumentViewer.tsx, src/components/transactions/MergeApprovedDocuments.tsx, PROJECT_STATUS.md. npm run build עבר. בדיקת PDF בדפדפן המשתמשת עדיין נדרשת: השוואת שני קבצים, גלילה, כפתורי אישור/ביטול ללא כיסוי, חלונית צרה ומובייל. אין SQL נוסף.

## מנוע חישוב מרכזי — 23/09/2026

מומש src/lib/calc.ts. ארבע הפונקציות הציבוריות: getReportingPeriods, getCurrentPeriod, summarize, compareToPrevious. כל תוצאות הכסף מוחזרות באגורות שלמות; הקלט של תנועות נשאר בשקלים כפי שהוא נשמר במסד הנתונים. אין שינוי בטבלאות או ב-RLS.

- תקופות מחזירות start/end בפורמט YYYY-MM-DD ושם עברי. Date מפורש באזור הזמן של ישראל.
- summarize מקבל options עם incomeTaxAdvanceRate (ברירת מחדל 0) ו-taxReserveRate (ברירת מחדל 30). על הקורא להעביר את ערכי הפרופיל כשמציגים מסים.
- כל החישובים הכספיים משתמשים בשברים שלמים ו-BigInt ובעיגול half-up. הכרה במע״מ מעוגלת בכל תנועה בנפרד. הפלט number שלם בטווח בטוח; חריגה נעצרת.
- byCategory הוא פירוט ההוצאות לפי categoryId, כולל null ללא קטגוריה. vatAmount הוא המע״מ לפני אחוז ההכרה; percentOfExpenses מחושב לפי ההוצאה לפני מע״מ. אין בו מע״מ של מטבע זר.
- תנועות לא מאושרות נכללות ומוחזר unverifiedCount. הסינון לפי משתמש וטווח תאריכים נשמר.
- בתנועות דולר שהומרו, amount_* הקיימים כבר בשקלים: נכללים בהכנסות ובהוצאות, אך המע״מ שלהם מוחרג, ומוחזר foreignCurrencyCount. רשומות ישנות עם currency_review_required אינן מצטרפות לסכומי השקלים, כפי שהיה קודם; מוחזר גם currencyReviewCount.
- compareToPrevious מחזיר אחוזים בשתי ספרות: 0 מול 0 מחזיר 0; ערך שונה מאפס מול 0 מחזיר null (אין אחוז מוגדר). בסיס שלילי משתמש בערך המוחלט של התקופה הקודמת.
- splitVat ו-addVat משמשים את טופס התנועה ואת אימות תוצאת החילוץ; convertMoney ו-shekelMoney מרוכזים כאן ומיוצאים מחדש מקובץ המטבע לתאימות.
- reporting.ts הוא מתאם בלבד למנוע. מסכי התנועות והתאריכון מציגים אגורות דרך formatCentsILS. שמירה למסד הנתונים ממשיכה בשקלים, numeric(12,2). בדיקות האיזון ב-SQL נשארות כהגנת שלמות נתונים.
- לא נבנו מסכים חדשים ולא השתנה פרומפט החילוץ.

קבצים חדשים:
- src/lib/calc.ts
- src/lib/calc.test.ts
- vitest.config.ts

קבצים ששונו:
- package.json, package-lock.json, .gitignore
- scripts/test-transactions.cjs, scripts/test-currency.cjs
- src/lib/currency/money.ts, src/lib/extraction/validate-result.ts, src/lib/format.ts
- src/lib/transactions/reporting.ts, load-range.ts, use-transaction-form.ts, validate-values.ts, document-matching.ts
- src/components/transactions/TransactionResults.tsx
- src/app/(app)/calendar/page.tsx, src/app/(app)/transactions/page.tsx
- PROJECT_STATUS.md

פקודות:
```sh
npm install -D vitest@4.0.18
npm test
node scripts/test-transactions.cjs
node scripts/test-currency.cjs
node scripts/test-merge-approved.cjs
npx eslint src vitest.config.ts
npm run build
npm run dev
```

Vitest נבחר בגרסה תואמת לתלויות הפרויקט. 14 בדיקות מכסות הכנסה והוצאה, הכרה 66% ו-25%, יתרת מע״מ שלילית, תקופה ריקה, 117.65 עם 18%, עיגול חצי אגורה, מטבע זר, תנועות לא מאושרות, קטגוריות, תקופות ושנה מעוברת, השוואה לאפס ובסיס שלילי וחריגה מטווח בטוח.
בדיקת npm run lint הכללית מדווחת על require בסקריפטי cjs הישנים; יש להבדיל בינה לבין בדיקת קוד האפליקציה והבדיקות החדשות.

בדיקות ידניות:
1. הכנסה 118 ₪ והוצאה 59 ₪, שתיהן ב-18% והכרה מלאה: יתרת המע״מ 9 ₪. השווי תנועות ותאריכון באותו טווח.
2. הוצאה עם מע״מ 18 ₪ בהכרה 66% ועוד אחת עם מע״מ 18 ₪ בהכרה 25%: מע״מ תשומות 16.38 ₪.
3. תקופה ריקה: אפסים. תקופה עם הוצאות בלבד: יתרת מע״מ שלילית.
4. בטופס: 117.65 ₪ כולל מע״מ ב-18% נותן 99.70 ₪ לפני מע״מ ו-17.95 ₪ מע״מ. עריכת מע״מ ידנית נשמרת, ושדה לא תקין אינו מפיל את הטופס.
5. תנועה בדולר עם שער שמור: הסיכומים הכספיים משתמשים בשקלים שכבר נשמרו, המע״מ מוחרג ומוצגת הודעה. שמירה בשקלים אינה דורשת שער חיצוני.
6. תנועה לא מאושרת קיימת נכללת עם הודעה; אין ליצור או לשנות נתוני אמת רק לצורך הבדיקה.
7. מעבר ינואר–פברואר למרץ–אפריל, וכן חודש פברואר בשנה מעוברת.
8. התחברות עם משתמש אחר: התנועות של המשתמש הראשון אינן מוצגות.

## דשבורד — 23/09/2026

נבנה /dashboard, שאליו עמוד הבית מפנה משתמשת מחוברת.
- כותרת עם שם העסק ובורר תקופה: דיווח לפי הפרופיל, חודש, שנה וטווח מותאם. ההחלה בכפתור ״הצג תקופה״, עם מצב טעינה.
- ארבעה מדדים עם השוואה לתקופה הקודמת; יתרת מע״מ שלילית מוצגת כהחזר ירוק. בסיס אפס מוצג כהיעדר בסיס להשוואה.
- פירוט מע״מ, מקדמות וכרית מס לפי הפרופיל, ימים לסיום וקישור לייצוא עם הטווח. הייצוא עצמו עדיין לא ממומש.
- Recharts: הכנסות והוצאות לפני מע״מ ב־12 חודשים, טולטיפ מדויק, מקלדת וטבלת נתונים חלופית. הגרף מסתיים בתאריך המוקדם מבין סוף הבחירה והיום; כך אין חודשים עתידיים כשנבחרת השנה הנוכחית.
- קטגוריות ממוינות לפי ההוצאה לפני מע״מ, ו־5 תנועות אחרונות לפי תאריך המסמך בטווח.
- דורש טיפול: מסמכים לאישור/כישלון זיהוי לפי יום ההעלאה בטווח; ללא קטגוריה והכרה בסוג לא מזכה לפי תאריך התנועה בטווח. מסמכים מאושרים או שהוסרו אינם מוצגים שוב. לכל פריט קישור למסך הבדיקה או פרטי התנועה עם כפתור עריכה.
- פתיחה ידידותית רק כשאין תנועות כלל בחשבון, ולא כשהטווח הנבחר ריק. אם כבר הועלו מסמכים יש גם קישור להמשך אישור.

חישובים: summarize, compareToPrevious ופונקציות התקופות ב-calc.ts. נוספו באותו מודול getDashboardWindow ו-summarizeMonths, שמשתמשת ב-summarize; אין נוסחאות פיננסיות חדשות במסך. שדות כסף מהמסד נשארים בשקלים והסיכומים באגורות.
השוואה: דיווח לדיווח קודם, חודש לחודש קודם, שנה לשנה קודמת; טווח מותאם לטווח שקדם לו באותו מספר ימים. ההשוואה היא טווח מלא מול מלא, גם אם התקופה הנוכחית טרם הסתיימה, ומופיע הסבר בממשק.
טעינה בצד שרת בלבד, טווחים מוגדרים ובעימוד לשמירה על כל הרשומות. ספירת קיום בלבד נעשית על כלל תנועות המשתמש עבור מסך הפתיחה. כל שאילתות הטבלאות עם user_id; profiles עם id של המשתמש; לקוח המשתמש ו-RLS נשמרים. אין שינוי בטבלאות ואין SQL להרצה.

נוצרו:
- src/app/(app)/dashboard/loading.tsx
- src/components/dashboard/DashboardView.tsx
- src/components/dashboard/PeriodPicker.tsx
- src/components/dashboard/MonthlyChart.tsx
- src/components/dashboard/DashboardView.test.tsx
- src/lib/dashboard/load-dashboard.ts
- src/lib/dashboard/dashboard.test.ts

שונו:
- src/app/(app)/dashboard/page.tsx
- src/lib/calc.ts
- src/lib/format.ts
- src/lib/transactions/review-queue.ts (טווח העלאה אופציונלי; הקוראים הקיימים ללא שינוי)
- package.json, package-lock.json (Recharts בלבד)
- vitest.config.ts (בדיקות רכיבי TSX)
- PROJECT_STATUS.md

אימות:
- 26 בדיקות Vitest עברו.
- scripts/test-transactions.cjs ו-scripts/test-currency.cjs עברו.
- npx eslint src vitest.config.ts ו-npm run build עברו.
- נבדקה תצוגה מקומית עם נתונים מדומים בדפדפן ב־1440 וברוחב 390: ללא גלילה אופקית; טווח מותאם חושף תאריכים; מקלדת מפעילה את טולטיפ הגרף. זו בדיקת רכיבים, לא בדיקת כניסה לחשבון אמיתי. ניווט וטעינת נתוני אמת תחת RLS דורשים בדיקה ידנית עם משתמשת מחוברת.
- לא בוצע פרסום לאתר החי במסגרת השלב הזה.

פקודות:
```sh
npm install
npm test
node scripts/test-transactions.cjs
node scripts/test-currency.cjs
npx eslint src vitest.config.ts
npm run build
npm run dev
```
התקנת התלות בנפרד, אם צריך: npm install recharts.
תיעוד הגרף: https://recharts.github.io/en-US/api/BarChart/

בדיקות ידניות:
1. התחברי ופתחי את עמוד הבית: שם העסק ותקופת הדיווח מההגדרות מוצגים.
2. החליפי חודש, שנה וטווח מותאם ולחצי הצג תקופה. בדקי סיכומים, תנועות, קטגוריות וקישורי הייצוא.
3. השווי לתנועות באותו טווח; מדדי הכנסה/הוצאה בדשבורד לפני מע״מ, ובתנועות הסיכום הראשי כולל מע״מ.
4. בדקי החזר מע״מ ירוק ובסיס השוואה אפס.
5. בדקי מקדמות וכרית מס מול האחוזים ששמרת בהגדרות.
6. בדקי טולטיפ עם עכבר/מקלדת/נגיעה וטבלת הגרף. בדקי שאין עמודות של חודשים עתידיים.
7. בדקי קישורים של כל סוגי ״דורש טיפול״; מסמך שאושר או הוסר לא חוזר לרשימה.
8. בדקי עסק חדש ללא תנועות, ועסק קיים עם טווח ריק.
9. בדקי בטלפון שאין גלילה אופקית או שמות שנשברים לאותיות, וכן במצב כהה.
10. התחברי במשתמש אחר וודאי שנתוני העסק הראשון אינם מופיעים.

## בחירת מדדים והוצאות כולל מע״מ — 23/09/2026

נוסף כרטיס ״הוצאות כולל מע״מ״ המשתמש ב-totals.expenseTotal ובהשוואה comparison.expenseTotal ממנוע calc.ts, ללא נוסחה חדשה. זה סכום המסמכים שנרשמו, ולא בהכרח תשלומים ששולמו בפועל. כרטיס ״לפני מע״מ״ נשאר זמין בנפרד.

נוסף בורר ״בחירת מדדים לתצוגה״ מעל הכרטיסים. רשימת המדדים מרוכזת ב-metrics.ts כדי שיהיה אפשר להוסיף בעתיד מדדים שאושרו. ניתן להציג/להסתיר כל אחד מחמשת המדדים וללחוץ ״שמור תצוגה״. ברירת המחדל כוללת גם את הכרטיס החדש. מותר להסתיר את כל הכרטיסים, ואז מוצגת דרך להוסיפם שוב.

הבחירה נשמרת לשנה בעוגייה בדפדפן, במפתח נפרד לפי מזהה המשתמש המחובר. אינה מסתנכרנת בין מכשירים. השרת קורא אותה לפני הרינדור; פעולת השמירה מאמתת משתמש ובחירה, ואינה מקבלת מזהה משתמש מהלקוח. שמורים רק מזהי מדדים, לא סכומים או מסמכים. ללא שינוי בסכמה, RLS או חישובי המס. שינוי התצוגה אינו משנה את סיווג העסק או את מודל המס שלו.

הסבר הנוסחאות הקיימות:
- כרית מס = הרווח החיובי לפני מס כפול tax_reserve_rate / 100. ברירת מחדל 30%; ברווח שלילי או אפס הכרית אפס. זו הצעת סכום לשמירה בצד, לא חישוב מדרגות מס או חיוב רשמי.
- מקדמות = הכנסות לפני מע״מ כפול income_tax_advance_rate / 100, ברירת מחדל 0. זה אומדן גולמי לפי האחוז שהמשתמשת הזינה. אין קיזוז אוטומטי של ניכוי במקור או מקדמות ששולמו.
- הכרית אינה מופחתת במקדמות: אין לחבר את שני המדדים כאילו היו שני חיובי מס נפרדים.
- דוגמה: הכנסות לפני מע״מ 20,000 ₪, הוצאות לפני מע״מ 10,000 ₪, כרית 30%, מקדמות 5%: כרית 3,000 ₪, אומדן מקדמות 1,000 ₪.

נוצרו:
- src/components/dashboard/MetricCards.tsx
- src/lib/dashboard/metrics.ts
- src/lib/dashboard/metric-actions.ts
- src/lib/dashboard/metrics.test.ts

שונו:
- src/app/(app)/dashboard/page.tsx
- src/components/dashboard/DashboardView.tsx
- src/components/dashboard/DashboardView.test.tsx
- PROJECT_STATUS.md

בדיקות: 30 בדיקות Vitest, ESLint לקוד המקור ובניית הפרויקט עברו.
פקודות: npm test; npx eslint src; npm run build; npm run dev.
ללא ספריות חדשות או SQL. טרם פורסם לאתר החי במסגרת שינוי זה.

בדיקות ידניות:
1. חשבונית הוצאה של 100 ₪ ועוד 18 ₪ מע״מ מציגה 100 ₪ לפני מע״מ ו-118 ₪ כולל.
2. הוצאה ללא מע״מ מציגה סכום זהה בשני הכרטיסים.
3. הסתירי את כרטיס ״לפני מע״מ״, שמרי ורענני. הבחירה צריכה להישמר גם אחרי שינוי תקופה.
4. בדקי שהשוואת ההוצאות הכוללות משתמשת בסכומים הכוללים של התקופה הקודמת.
5. בדקי שמשתמש אחר באותו דפדפן אינו מקבל את בחירת התצוגה שלך.
6. הסתירי את כל הכרטיסים והחזירי דרך ״הצג את כל המדדים״ ואז ״שמור תצוגה״.

## מסך ייצוא לרו״ח — 23/09/2026

נבנה מסך /export (עד עכשיו טעון בלבד). בחירת טווח: יומי, שבועי, חודשי, תקופת הדיווח הנוכחית (לפי reporting_frequency בהגדרות), שנתי, או טווח מותאם — כל הפרסטים מלבד יומי/שבועי/מותאם משתמשים ב-getCurrentPeriod הקיים ב-calc.ts, בלי נוסחת תאריכים כפולה. תצוגה מקדימה מציגה מספר תנועות, סה״כ הכנסות/הוצאות ומע״מ לתשלום לפני ייצוא, ומזהירה על תנועות שדורשות בדיקת מטבע (מוחרגות מהייצוא כליל, כמו בדשבורד) ועל תנועות במטבע זר (נכללות בהמרה לשקלים אך לא במע״מ, כמו בכל שאר המערכת).

שלושה פלטים כמבוקש, ועוד כפתור ״ייצא הכל״:
- אקסל (src/lib/export/build-workbook.ts, ספריית xlsx) — שלושה גיליונות (הוצאות/הכנסות/סיכום), RTL, עמודות ברוחב אוטומטי, פורמט מטבע לתאים מספריים, ניסיון הדגשה (בולד) על שורת כותרת ושורת סיכום — ייתכן שלא יוצג ב-Excel כי הגרסה החינמית של xlsx לא בהכרח כותבת עיצוב תאים; אם זה משנה אפשר לעבור בעתיד ל-xlsx-js-style. שורת הסיכום בכל גיליון היא סכימה מדויקת (agorot, לא float) של העמודה שמעליה; גיליון ה״סיכום״ הנפרד משתמש בערכים הרשמיים מ-calc.ts (זהים לדשבורד).
- דוח להדפסה/PDF (/export/report, נפתח בלשונית חדשה וקורא ל-window.print() אוטומטית) — בלי ספריית PDF כמבוקש; הדפדפן פותר את העברית. ה-sidebar/header מוסתרים בהדפסה בלבד (print:hidden ב-AppShell), לא במסך הרגיל.
- ZIP מסמכים (src/lib/export/build-zip.ts, ספריית jszip) — תיקיות הוצאות/ והכנסות/, שם קובץ תאריך_ספק_סכום, ו-index.csv בשורש. תנועה עם שני מסמכים מקושרים (חשבונית+קבלה שמוזגו) מקבלת שם עם מונה (2) כדי למנוע דריסה.
- ״ייצא הכל״ (/api/export/bundle) — ZIP אחד עם קובץ האקסל ביחד עם כל המסמכים. הדוח להדפסה לא יכול להצטרף ל-ZIP הזה כי אין ספריית PDF (כפי שהתבקש), אז הוא נשאר כפתור נפרד שפותח לשונית ומדפיס.

מספר עוסק של הספק בגיליון ההוצאות מגיע מ-documents.extraction_raw.business_number של המסמך המצורף לתנועה (השדה כבר נאסף בחילוץ בעבר לצורך התאמת מסמכים, ועכשיו נחשף גם בייצוא) — אין עמודה כזו בטבלת transactions, אז אם לתנועה אין מסמך מצורף עם השדה הזה התא נשאר ריק.

״סימון תקופה כהוגשה״ מוגבל לפרסט ״תקופת דיווח״ בלבד (לא יומי/שבועי/מותאם) — נוצרת/מתעדכנת שורה בטבלת periods הקיימת (status='closed', snapshot עם הסיכום המלא). מסך /export/history מציג את כל התקופות שהוגשו. בעריכת תנועה (TransactionDrawer) שתאריך המסמך שלה נופל בתוך תקופה סגורה מופיעה אזהרה — העריכה עצמה לא נחסמת, רק מוצגת אזהרה (getDetail ב-detail-actions.ts מוסיף שדה lockedPeriod).

תנועות עם currency_review_required מוחרגות לגמרי מכל הייצוא (אקסל/ZIP/דוח/סיכום) — לא רק מהסכומים, גם מרשימת השורות — כי הנתונים שלהן לא סופיים.

ספריית xlsx (גרסת npm, לא xlsx-js-style) עם אזהרת אבטחה ידועה (prototype pollution / ReDoS ב-npm audit) שאין לה תיקון רשמי ב-npm — היא רלוונטית רק לקריאת קבצים חיצוניים לא מהימנים, והשימוש כאן הוא כתיבה בלבד (בניית האקסל מנתונים מהמסד), כך שהחשיפה בפועל נמוכה. מוזכר כאן לשקיפות.

נוצרו:
- src/lib/export/range.ts, range.test.ts
- src/lib/export/filenames.ts
- src/lib/export/load-export-data.ts
- src/lib/export/build-workbook.ts, build-workbook.test.ts
- src/lib/export/build-zip.ts
- src/lib/export/route-helpers.ts
- src/lib/export/period-actions.ts
- src/app/api/export/excel/route.ts
- src/app/api/export/zip/route.ts
- src/app/api/export/bundle/route.ts
- src/components/export/ExportFilters.tsx, ExportButtons.tsx, SubmitPeriodButton.tsx, PrintOnLoad.tsx, PrintReport.tsx
- src/app/(app)/export/report/page.tsx
- src/app/(app)/export/history/page.tsx

שונו:
- src/app/(app)/export/page.tsx (מ-stub למסך מלא)
- src/components/layout/AppShell.tsx (print:hidden ל-aside/header)
- src/components/transactions/TransactionDrawer.tsx (אזהרת תקופה נעולה)
- src/lib/transactions/detail-actions.ts (lockedPeriod ב-Detail)
- package.json, package-lock.json (xlsx, jszip — התבקשו במפורש)
- PROJECT_STATUS.md

בדיקות: 41 בדיקות Vitest (כולל 11 חדשות), npx tsc --noEmit, npx eslint src (0 שגיאות בקבצים החדשים — 15 השגיאות הקיימות הן ב-scripts/*.cjs הישנים, לא קשורות), npm run build עברו.
פקודות:
```sh
npm install
npm test
npx tsc --noEmit
npx eslint src
npm run build
npm run dev
```

בדיקות ידניות שביצעתי בפועל (משתמש בדיקה זמני עם נתונים אמיתיים ב-Supabase, נמחק בסוף):
1. תצוגה מקדימה לתקופת ספט-אוק 2026 עם 4 תנועות (אחת דורשת בדיקת מטבע): הוצגו נכון 3 תנועות, ₪1,180 הכנסות, ₪826 הוצאות, ₪66.24 מע״מ לתשלום — הצלבתי ידנית מול הנוסחה (מע״מ תשומות = 90+23.76=113.76, מע״מ עסקאות 180, הפרש 66.24).
2. הורדתי בפועל (fetch אמיתי דרך הדפדפן) את שלושת הקבצים: אקסל תקין (חתימת PK, שלושה גיליונות בשמות נכונים, ערכים תואמים לתצוגה המקדימה בול), ZIP מסמכים (הוצאות/ הכנסות/ index.csv), ZIP ״הכל״ (כולל גם את קובץ האקסל). שם הקובץ היוצא בעברית תקין (חשבוניות_עסק לבדיקת ייצוא_ספטמבר–אוקטובר 2026.xlsx).
3. /export/report הציג טבלאות נכונות (זהות לגיליון הסיכום) והפעיל window.print() אוטומטית.
4. ״סימון התקופה כהוגשה״ יצר שורה ב-periods; הכפתור החליף למצב ״כבר סומנה״; התקופה הופיעה נכון ב-/export/history.
5. פתיחת תנועה מהתקופה שהוגשה הציגה את אזהרת ״תקופת דיווח שכבר הוגשה״.
6. בחרתי ״יומי״ בבורר טווח והצגתי תצוגה ריקה (אין תנועות) — כל כפתורי הייצוא הושבתו נכון כשאין מה לייצא.
7. בדיקת יחידה (vitest) לכל פרסטים של resolveExportRange ולבניית האקסל, כולל אימות סכומים מדויקים מתוך קובץ xlsx אמיתי שנקרא בחזרה.

לא בוצע: פרסום לאתר החי במסגרת השלב הזה (ממתין לאישור).

## PWA והתאמה לנייד — 24/09/2026

הפכתי את האפליקציה לניתנת להתקנה (PWA) והתאמתי את כל המסכים לרוחב 375 פיקסלים, לפי הבקשה. נבדק בפועל בדפדפן בוויופורט 375×812 עם משתמש בדיקה אמיתי (נמחק בסוף), כולל דשבורד, תנועות, תאריכון, ומסך אישור מסמך.

מניפסט והתקנה: `src/app/manifest.ts` (המנגנון המובנה של Next — מוגש אוטומטית ב-/manifest.webmanifest) עם שם "חשבוניות", standalone, צבעי רקע/נושא, ו-shortcut "צלם חשבונית" ל-/upload?mode=camera. באייקונים (192, 512, וגם גרסת maskable ל-Android ו-apple-touch-icon ל-iPhone) — ציירתי אותם בעצמי בקוד עם zlib המובנה של Node (בלי ספריית תמונות חדשה): רקע בצבע הראשי של האפליקציה וסמל קבלה עם קווי טקסט. ב-layout.tsx נוספו תגי ה-meta הנדרשים ל-iPhone: apple-mobile-web-app-capable, apple-mobile-web-app-title, apple-mobile-web-app-status-bar-style, ו-viewport-fit=cover (בדקתי בדפדפן שכל התגים אכן מופיעים ב-head).

מסך /upload?mode=camera פותח אוטומטית את המצלמה כשהיא זמינה במכשיר (משתמש בכפתור "צלמי מסמך" הקיים ב-DropZone, לא נבנה מנגנון נפרד) — דרש פיצול page.tsx ל-Server Component דק שקורא את הפרמטר וקומפוננטת קליינט נפרדת (UploadPageClient.tsx) כדי לקרוא את ה-query param בלי useSearchParams. בדיקה אמיתית של פתיחת מצלמה אפשרית רק בטלפון אמיתי — בסביבת הבדיקה שלי (דפדפן אוטומטי) אין מצלמה אמיתית, כך שהלוגיקה נבדקה שאינה זורקת שגיאה אך לא את פתיחת המצלמה בפועל.

תפריט תחתון קבוע (MobileBottomNav.tsx) עם 4 קישורים כמבוקש (דשבורד/העלאה/תנועות/תאריכון), וכפתור צף עגול (FloatingUploadButton.tsx) שמוביל ל-/upload?mode=camera — שניהם מוצגים רק מתחת ל-md ומוסתרים בהדפסה. הכפתור הצף מוסתר גם במסך /upload עצמו (יש שם כבר כפתור מצלמה) וגם במסך אישור מסמך (יש שם כבר שורת כפתורי שמירה צמודה לתחתית, והם היו מתנגשים חזותית). הוספתי ריווח תחתון למסך הראשי (main) כדי שתוכן לא יישאר תקוע מתחת לתפריט/לכפתור בסוף הגלילה, והזזתי כלפי מעלה את שתי שורות הכפתורים הדביקות הקיימות שהיו מתנגשות עם התפריט התחתון החדש: שורת הסיכומים בתנועות/תאריכון ושורת "אשר ושמור" במסך האישור.

התאמות רוחב 375px: טבלת התנועות (TransactionResults.tsx) מקבלת כעת תצוגת כרטיסים נפרדת מתחת ל-md (הטבלה המקורית עדיין קיימת ומוצגת מ-md ומעלה, ללא שינוי בהתנהגות שלה). כרטיסי הדשבורד (MetricCards.tsx) עברו מעמודה אחת/שתיים לשתי עמודות קבועות במובייל (היו מתקפלים לעמודה אחת מתחת ל-sm). התאריכון ומסך האישור כבר היו בנויים נכון לרוחב הזה (התאריכון גמיש מראש; מסך האישור כבר כלל לשוניות "מסמך"/"פרטים לאישור" למובייל) — נבדק ולא נדרש שינוי.

גודל גופן בשדות קלט: כלל CSS גלובלי לא-מכוסה-שכבה (`input, select, textarea { font-size: 16px; }` ב-globals.css) שגובר על text-sm של Tailwind בזכות סמנטיקת ה-layers של Tailwind v4 — נבדק בדפדפן: כל 12 שדות הקלט בטופס תנועה מציגים 16px בדיוק. זה מונע זום אוטומטי באייפון בלי לגעת בכל קומפוננטה בנפרד.

Service Worker מינימלי (public/sw.js, נרשם דרך src/components/pwa/RegisterServiceWorker.tsx): לא שומר שום דבר במטמון ואינו תומך בעבודה לא מקוונת מלאה כפי שהתבקש — הוא רק תופס בקשות ניווט (טעינת עמוד) שנכשלות בגלל היעדר רשת ומחזיר עמוד הודעה קבוע "אין חיבור לאינטרנט" עם כפתור "נסי שוב" במקום מסך השגיאה של הדפדפן. נבדק שהוא נרשם בהצלחה בדפדפן; בדיקת ניתוק רשת אמיתית אפשרית רק במכשיר אמיתי (מצב טיסה) ולא בסביבת הבדיקה האוטומטית שלי.

הודעות מערכת: עברתי בגריפ על כל ההודעות המסתיימות ב"נכשל" או "לא ניתן" בקוד. רוב ההודעות כבר כוללות הנחיה לפעולה (נסי שוב / רענני) או מוצגות ליד כפתור נסיון חוזר קיים. תיקנתי שלוש הודעות שהיו חסרות זאת לגמרי (חיפוש חשבוניות, צירוף קבלה, חישוב מטבע בחיבור מסמכים) — הוספתי "נסי שוב" בסוף.

נוצרו:
- src/app/manifest.ts
- public/icons/icon-192.png, icon-512.png, icon-maskable-512.png, apple-touch-icon.png
- public/sw.js
- src/components/pwa/RegisterServiceWorker.tsx
- src/components/layout/MobileBottomNav.tsx, FloatingUploadButton.tsx
- src/app/(app)/upload/UploadPageClient.tsx

שונו:
- src/app/layout.tsx (metadata, viewport, manifest, appleWebApp, רישום ה-service worker)
- src/app/globals.css (16px לשדות קלט, מחלקת safe-area-bottom)
- src/app/(app)/upload/page.tsx (עטיפת שרת דקה שקוראת mode=camera)
- src/components/upload/DropZone.tsx (פתיחת מצלמה אוטומטית כש-autoOpenCamera=true)
- src/components/layout/AppShell.tsx (תפריט תחתון, כפתור צף, ריווח תחתון ל-main)
- src/components/transactions/TransactionResults.tsx (תצוגת כרטיסים למובייל, מיקום שורת הסיכום)
- src/components/dashboard/MetricCards.tsx (שתי עמודות במובייל, גדלים מותאמים), DashboardView.test.tsx (עדכון בדיקה קיימת לקלאסים החדשים)
- src/app/(app)/documents/[id]/review/page.tsx (מיקום שורת הכפתורים הדביקה מעל התפריט התחתון)
- src/lib/transactions/attach-receipt.ts, pair-actions.ts (שלוש הודעות שגיאה)
- PROJECT_STATUS.md

בדיקות: 41 בדיקות Vitest, npx tsc --noEmit, npx eslint src, npm run build — כולן עברו.
פקודות:
```sh
npm install
npm test
npx tsc --noEmit
npx eslint src
npm run build
npm run dev
```

בדיקות ידניות שביצעתי בפועל (וויופורט 375×812, משתמש בדיקה זמני שנמחק בסוף):
1. דשבורד: כרטיסי המדדים בשתי עמודות, בלי גלילה אופקית; גרף ההכנסות/הוצאות מצטמצם לרוחב המסך.
2. תנועות ותאריכון: רשימת כרטיסים במקום טבלה; שורת הסיכום התחתונה נשארת מעל התפריט התחתון בלי חפיפה.
3. מסך אישור מסמך: לשוניות "מסמך"/"פרטים לאישור" עובדות; שורת "אשר ושמור" נשארת מעל התפריט התחתון; אין כפתור צף מיותר במסך הזה.
4. תפריט תחתון קבוע עם 4 הקישורים המבוקשים, מסמן נכון את המסך הפעיל; כפתור צף פותח /upload?mode=camera ומוסתר נכון ב-/upload ובמסך האישור.
5. manifest.webmanifest מכיל את כל השדות (שם עברי, standalone, אייקונים, ה-shortcut); כל תגי ה-meta הנדרשים ל-iPhone קיימים ב-head; קבצי האייקונים נטענים בהצלחה.
6. Service Worker נרשם בהצלחה (navigator.serviceWorker.getRegistration).
7. כל 12 שדות הקלט בטופס תנועה חדשה מודדים 16px בדיוק (מונע זום אוטומטי באייפון).

בדיקות שדורשות מכשיר אמיתי (לא ניתנות לבדיקה בסביבה האוטומטית שלי):
- פתיחת מצלמה בפועל דרך /upload?mode=camera או ה-shortcut במסך הבית.
- "הוספה למסך הבית" באייפון ואנדרואיד, ושהאפליקציה נפתחת ב-standalone (בלי סרגל כתובת).
- מצב טיסה: שהאפליקציה מציגה את עמוד "אין חיבור לאינטרנט" במקום שגיאת דפדפן.
- שאין זום אוטומטי בפועל בהקלדה בשדה בטלפון אמיתי.
