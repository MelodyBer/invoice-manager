-- ============================================================================
-- מיגרציה 2: הסרת הבלימה הקשיחה על כפילויות בטבלת transactions
-- להריץ פעם אחת ב-Supabase: SQL Editor -> New query -> להדביק -> Run
-- ============================================================================
--
-- הסבר: בשלב בניית מסד הנתונים יצרנו אילוץ ייחודיות (transactions_dedupe_idx)
-- שמנע לגמרי שמירה כפולה של אותה תנועה (אותו ספק + מספר מסמך + תאריך).
--
-- במסך "אישור מסמך" ביקשת אפשרות ל"שמור בכל זאת" כשהמערכת מזהה כפילות
-- אפשרית — כלומר לתת למשתמשת להחליט במודע לשמור בכל זאת. אילוץ קשיח ברמת
-- מסד הנתונים לא יכול לאפשר "עקיפה" מבוקרת כזו, ולכן הוא היה חוסם את
-- הפעולה גם כשהמשתמשת אישרה מפורשות שהיא רוצה לשמור בכל זאת.
--
-- הבדיקה הקיימת (השוואה מול תנועות קיימות והצגת דיאלוג אזהרה) עברה
-- לרמת האפליקציה במסך האישור, ולכן האילוץ הקשיח במסד הנתונים כבר לא נחוץ.
-- ============================================================================

begin;
drop index if exists public.transactions_dedupe_idx;
create index transactions_dedupe_idx
  on public.transactions (user_id, counterparty_name, doc_number, doc_date);
commit;
