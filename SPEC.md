# SPEC.md — מפרט מערכת ניהול חשבוניות לעוסק מורשה

> מסמך אפיון בלבד. אין בו קוד. מטרתו לתאר את המערכת במלואה כבסיס לעבודה בשלבים.

---

## 1. סקירה כללית

מערכת web לניהול חשבוניות, הכנסות והוצאות של עוסק מורשה בישראל. המערכת מאפשרת העלאת מסמכים (הוצאה/הכנסה), חילוץ אוטומטי של הנתונים מתוכם באמצעות Claude, אישור/תיקון ידני של הנתונים, מעקב לפי תאריכון, דשבורד מצב שוטף, וייצוא מרוכז לרואה החשבון.

**סטאק:** Next.js 15 (App Router) + TypeScript, Tailwind CSS v4, Supabase (Auth + Postgres + Storage + RLS), Anthropic API (`claude-sonnet-5`) לחילוץ נתונים, פריסה ב-Vercel.

**שפה וכיוון:** כל טקסט בממשק בעברית, `dir="rtl"` מלא. מטבע: ₪. תאריכים: `DD/MM/YYYY`. שמות טכניים (משתנים, קבצים, טבלאות, עמודות) — באנגלית.

**מע״מ:** 18%, דיווח דו-חודשי (ינואר–פברואר, מרץ–אפריל, מאי–יוני, יולי–אוגוסט, ספטמבר–אוקטובר, נובמבר–דצמבר).

---

## 2. מבנה מסד הנתונים (Supabase / Postgres)

כל הטבלאות (למעט `categories` המערכתיות) מכילות `user_id` וכפופות ל-RLS: משתמש רואה ורושם רק שורות שבהן `user_id = auth.uid()`.

### 2.1 `profiles`

פרופיל העסק של המשתמש. שורה אחת לכל משתמש, נוצרת אוטומטית עם ההרשמה.

| עמודה | טיפוס | חובה | משמעות |
|---|---|---|---|
| `id` | `uuid` (PK, = `auth.users.id`) | כן | מזהה המשתמש |
| `business_name` | `text` | כן | שם העסק המוצג במסמכי ייצוא |
| `business_id_number` | `text` | כן | מספר עוסק מורשה |
| `full_name` | `text` | לא | שם בעל העסק |
| `vat_deductible_default_percent` | `numeric(5,2)`, ברירת מחדל `100` | כן | אחוז ברירת מחדל להכרה במע״מ תשומות עבור הוצאה חדשה |
| `income_tax_advance_percent` | `numeric(5,2)` | לא | אחוז מקדמות מס הכנסה מהמחזור, כפי שנקבע ע״י רשות המסים/רואה החשבון |
| `estimated_income_tax_rate_percent` | `numeric(5,2)` | לא | שיעור מס משוער לצורך חישוב "כרית מס" (הפרשה מומלצת) |
| `created_at` | `timestamptz` | כן | מועד יצירת הפרופיל |
| `updated_at` | `timestamptz` | כן | מועד עדכון אחרון |

### 2.2 `documents`

כל קובץ שהועלה, לפני ואחרי חילוץ הנתונים. מסמך אחד יכול להוליד מספר שורות ב-`transactions` (למשל כשיש כמה שיעורי מע״מ באותו מסמך).

| עמודה | טיפוס | חובה | משמעות |
|---|---|---|---|
| `id` | `uuid` (PK) | כן | מזהה המסמך |
| `user_id` | `uuid` (FK → profiles) | כן | בעל המסמך, לסינון RLS |
| `storage_path` | `text` | כן | נתיב הקובץ ב-Supabase Storage |
| `file_type` | `text` — `jpg` / `png` / `pdf` | כן | סוג הקובץ שהועלה |
| `original_filename` | `text` | לא | שם הקובץ המקורי |
| `upload_source` | `text` — `file` / `camera` | לא | האם הועלה מקובץ קיים או צולם ישירות |
| `status` | `text` — `pending` / `processing` / `extracted` / `failed` / `confirmed` | כן | מצב עיבוד המסמך |
| `extraction_raw` | `jsonb` | לא | תשובת החילוץ הגולמית מ-Claude, לצורך debug/audit |
| `extraction_confidence` | `numeric(5,2)` | לא | רמת ביטחון בחילוץ (0–100), משמשת לזיהוי מסמכים "בעייתיים" |
| `error_message` | `text` | לא | פרטי שגיאה אם `status = failed` |
| `created_at` | `timestamptz` | כן | מועד העלאה |
| `updated_at` | `timestamptz` | כן | מועד עדכון אחרון |

### 2.3 `transactions`

התנועה הכספית בפועל, כפי שאושרה (או ממתינה לאישור) ע״י המשתמש. מקושרת למסמך המקור.

| עמודה | טיפוס | חובה | משמעות |
|---|---|---|---|
| `id` | `uuid` (PK) | כן | מזהה התנועה |
| `user_id` | `uuid` (FK → profiles) | כן | בעל התנועה, לסינון RLS |
| `document_id` | `uuid` (FK → documents) | כן | המסמך שממנו נוצרה התנועה |
| `direction` | `text` — `expense` / `income` | כן | הוצאה או הכנסה |
| `counterparty_name` | `text` | כן | שם הספק (בהוצאה) או הלקוח (בהכנסה) |
| `doc_number` | `text` | לא | מספר המסמך כפי שמופיע עליו |
| `doc_type` | `text` — `חשבונית מס` / `חשבונית מס-קבלה` / `קבלה` / `חשבונית עסקה` / `אחר` | כן | סוג המסמך |
| `doc_date` | `date` | כן | תאריך המסמך |
| `amount_before_vat` | `numeric(12,2)` | כן | סכום לפני מע״מ, בש״ח |
| `vat_amount` | `numeric(12,2)` | כן | סכום המע״מ, בש״ח |
| `amount_total` | `numeric(12,2)` | כן | סכום כולל, בש״ח |
| `vat_rate` | `numeric(5,2)`, ברירת מחדל `18` | כן | שיעור המע״מ שחל על התנועה |
| `vat_deductible_percent` | `numeric(5,2)`, ברירת מחדל `100` | כן | אחוז ההכרה במע״מ תשומות (רלוונטי בעיקר להוצאות) |
| `category_id` | `uuid` (FK → categories) | לא | סיווג התנועה |
| `notes` | `text` | לא | הערה חופשית |
| `is_verified` | `boolean`, ברירת מחדל `false` | כן | האם המשתמש אישר את הנתונים במסך האישור |
| `is_duplicate_of` | `uuid` (FK → transactions, nullable) | לא | הפניה לתנועה קיימת אם זוהתה/סומנה ככפילות |
| `original_currency` | `text`, ברירת מחדל `ILS` | לא | מטבע מקורי של המסמך אם שונה מש״ח |
| `original_amount_total` | `numeric(12,2)` | לא | סכום כולל במטבע המקורי |
| `exchange_rate` | `numeric(10,4)` | לא | שער שהומר לפיו לש״ח |
| `period_id` | `uuid` (FK → periods) | לא | תקופת הדיווח הדו-חודשית שאליה משויכת התנועה (מחושבת מ-`doc_date`) |
| `created_at` | `timestamptz` | כן | מועד יצירת התנועה |
| `updated_at` | `timestamptz` | כן | מועד עדכון אחרון |

### 2.4 `categories`

קטגוריות סיווג. שורות מערכתיות (`user_id IS NULL`) גלויות לכולם לקריאה בלבד; שורות משתמש (`user_id` מוגדר) הן קטגוריות מותאמות אישית.

| עמודה | טיפוס | חובה | משמעות |
|---|---|---|---|
| `id` | `uuid` (PK) | כן | מזהה הקטגוריה |
| `user_id` | `uuid` (FK → profiles, nullable) | לא | `NULL` = קטגוריית ברירת מחדל מערכתית; אחרת קטגוריה פרטית של המשתמש |
| `name` | `text` | כן | שם הקטגוריה (בעברית) |
| `direction` | `text` — `expense` / `income` / `both` | כן | לאיזה סוג תנועה הקטגוריה רלוונטית |
| `is_default` | `boolean`, ברירת מחדל `false` | כן | האם זו קטגוריית ברירת מחדל מוצעת |
| `created_at` | `timestamptz` | כן | מועד יצירה |

*רשימת קטגוריות הברירת המחדל (למשל: ציוד משרדי, רכב ודלק, שכירות, טלפון ותקשורת, שיווק ופרסום, ייעוץ מקצועי, מכירת מוצר, מתן שירות, אחר) תיקבע בשלב מימוש הטבלה — לא כעת.*

### 2.5 `periods`

תקופת דיווח דו-חודשית של מע״מ, לכל משתמש. נוצרת/מתעדכנת בעת שמירת תנועה בתקופה שאין לה עדיין שורה.

| עמודה | טיפוס | חובה | משמעות |
|---|---|---|---|
| `id` | `uuid` (PK) | כן | מזהה התקופה |
| `user_id` | `uuid` (FK → profiles) | כן | בעל התקופה, לסינון RLS |
| `year` | `integer` | כן | שנה |
| `period_number` | `integer` (1–6) | כן | מספר התקופה הדו-חודשית בשנה |
| `start_date` | `date` | כן | תחילת התקופה |
| `end_date` | `date` | כן | סוף התקופה |
| `status` | `text` — `open` / `closed` / `reported` | כן | מצב התקופה מבחינת דיווח |
| `vat_to_pay_snapshot` | `numeric(12,2)` | לא | תמונת מצב של מע״מ לתשלום, נשמרת בעת סגירת התקופה |
| `reported_at` | `timestamptz` | לא | מועד סימון "דווח" |
| `notes` | `text` | לא | הערה חופשית |
| `created_at` | `timestamptz` | כן | מועד יצירה |

### 2.6 RLS — עקרונות כלליים

- בכל טבלה עם `user_id`: `SELECT/INSERT/UPDATE/DELETE` מותרים רק כאשר `user_id = auth.uid()`.
- ב-`categories`: קריאה מותרת גם לשורות `user_id IS NULL`; כתיבה/מחיקה מותרת רק לשורות שבהן `user_id = auth.uid()`.
- ב-Storage: bucket פרטי, קבצים נשמרים תחת נתיב `documents/{user_id}/{document_id}/...`, ומדיניות ה-bucket מוודאת גישה רק לתיקיית המשתמש המחובר.

---

## 3. מסכים ונתיבים (App Router)

| נתיב | מסך | תיאור |
|---|---|---|
| `/` | ראוטינג | אם מחובר → הפניה ל-`/dashboard`, אחרת → `/login` |
| `/login` | התחברות | טופס התחברות (Supabase Auth) |
| `/signup` | הרשמה | טופס הרשמה + יצירת שורת `profiles` ראשונית |
| `/dashboard` | דשבורד | מסך הפתיחה לאחר כניסה: מצב התקופה הנוכחית, מע״מ עסקאות/תשומות/לתשלום עד כה, מחזור, רווח משוער, מסמכים ממתינים לאישור, כפתור העלאה מהיר |
| `/upload` | העלאת מסמך | בחירת סוג תנועה (הוצאה/הכנסה), העלאת קובץ (JPG/PNG/PDF) או צילום ישיר במובייל, שליחה לחילוץ |
| `/documents` | רשימת מסמכים | טבלה/רשימה של כל המסמכים עם סינון לפי סטטוס, כיוון, טווח תאריכים וחיפוש חופשי |
| `/documents/[id]` | מסך אישור/עריכה | הצגת תמונת/PDF המסמך לצד טופס הנתונים שחולצו; עריכה ותיקון; שמירה יוצרת/מעדכנת שורת `transactions` ומסמנת `is_verified = true` |
| `/calendar` | תאריכון | לוח שנה חודשי; לכל יום — מספר מסמכים וסכום כולל; ניווט בין חודשים |
| `/calendar/[date]` | יום בתאריכון | רשימת המסמכים/תנועות של יום ספציפי, עם אפשרות להעלות מסמך עם תאריך מוצע = היום שנבחר |
| `/export` | ייצוא לרואה חשבון | בחירת טווח תאריכים והפקת שלושת המסמכים: הוצאות מאוחד, הכנסות מאוחד, סיכום מע״מ ומס |
| `/settings` | הגדרות | פרטי עסק (`profiles`), אחוזי מע״מ/מקדמות/שיעור מס משוער |
| `/settings/categories` | ניהול קטגוריות | הצגת קטגוריות ברירת מחדל + יצירה/עריכה/מחיקה של קטגוריות מותאמות אישית |

---

## 4. מסע המשתמש — מצילום החשבונית ועד הדשבורד

1. **צילום/העלאה** — המשתמש נכנס ל-`/upload` (או מ-`/calendar/[date]`), בוחר כיוון (הוצאה/הכנסה) ומצלם/מעלה קובץ.
2. **שמירת המסמך הגולמי** — הקובץ נשמר ב-Storage תחת תיקיית המשתמש; נוצרת שורת `documents` עם `status = pending`.
3. **חילוץ נתונים** — הקובץ נשלח ל-Anthropic API (`claude-sonnet-5`); `status` עובר ל-`processing` ואז ל-`extracted` או `failed`. תוצאת החילוץ (שם ספק/לקוח, תאריך, מספר מסמך, סכומים, סיווג מוצע) נשמרת ב-`extraction_raw`, יחד עם `extraction_confidence`.
4. **מעבר אוטומטי למסך אישור** — המשתמש מועבר ל-`/documents/[id]`, שם השדות שחולצו ממולאים מראש בטופס לצד תצוגת המסמך המקורי.
5. **בדיקה ותיקון** — המשתמש עובר על כל שדה, מתקן במידת הצורך, בוחר קטגוריה, ומאשר.
6. **שמירה** — בלחיצה על "אישור ושמירה" נוצרת (או נוצרות, אם יש כמה שיעורי מע״מ) שורת `transactions` עם `is_verified = true`; `document.status` עובר ל-`confirmed`; מחושבת/מעודכנת שורת ה-`periods` הרלוונטית לפי `doc_date`.
7. **הופעה בדשבורד ובתאריכון** — התנועה החדשה נכללת מיידית בחישובי הדשבורד (מחזור, מע״מ, רווח) ומופיעה ביום המתאים בתאריכון.

---

## 5. נוסחאות חישוב

כל הנוסחאות מחושבות עבור תקופה נתונה (תקופת מע״מ דו-חודשית) או טווח תאריכים חופשי (למסך ייצוא), על סמך תנועות עם `is_verified = true` בלבד.

1. **מע״מ עסקאות (מע״מ על הכנסות):**
   `output_vat = Σ vat_amount` עבור תנועות עם `direction = income`

2. **מע״מ תשומות מוכר (מע״מ על הוצאות, לאחר הכרה חלקית):**
   `input_vat_deductible = Σ (vat_amount × vat_deductible_percent / 100)` עבור תנועות עם `direction = expense`

3. **מע״מ לתשלום/להחזר:**
   `vat_due = output_vat − input_vat_deductible`
   (חיובי = לתשלום לרשות המסים; שלילי = לקבלת החזר)

4. **מחזור (Turnover):**
   `turnover = Σ amount_before_vat` עבור תנועות עם `direction = income`

5. **הוצאות מוכרות (לצורך רווח):**
   `recognized_expenses = Σ (amount_before_vat × vat_deductible_percent / 100)` עבור תנועות עם `direction = expense`
   > *הערת פישוט:* בשלב זה נעשה שימוש באותו `vat_deductible_percent` גם לצורכי הכרה במס הכנסה וגם לצורכי מע״מ תשומות. פיצול לשני אחוזי הכרה נפרדים (מע״מ מול מס הכנסה) הוא הרחבה אפשרית לשלב עתידי.

6. **רווח לפני מס:**
   `profit_before_tax = turnover − recognized_expenses`

7. **מקדמות מס הכנסה:**
   `income_tax_advance = turnover × (profile.income_tax_advance_percent / 100)`

8. **כרית מס (הפרשה מומלצת):**
   `tax_cushion = (profit_before_tax × profile.estimated_income_tax_rate_percent / 100) − income_tax_advance`
   (הסכום המומלץ להפריש נוסף על המקדמות ששולמו, כדי לכסות את חבות המס הצפויה בסוף שנה)

---

## 6. מקרי קצה

| מקרה | טיפול מוצע |
|---|---|
| **מסמך מטושטש / לא קריא** | אם `extraction_confidence` נמוך מסף מוגדר, המסמך מסומן לתשומת לב מיוחדת במסך האישור (למשל תג "בדיקה ידנית נדרשת"); כל השדות נשארים עריכים חופשי, ואין שמירה עד שהמשתמש ממלא ידנית את השדות החסרים. |
| **מסמך במטבע זר** | אם הסכום המחולץ אינו בש״ח, שדות `original_currency`, `original_amount_total` ו-`exchange_rate` מוצגים במסך האישור; המשתמש מזין/מאשר שער המרה, והמערכת מחשבת את `amount_before_vat`/`vat_amount`/`amount_total` בש״ח בהתאם. |
| **חשבונית עם כמה שיעורי מע״מ** | מסך האישור מאפשר פיצול המסמך למספר שורות תנועה (כולן עם אותו `document_id`), שורה אחת לכל שיעור מע״מ, כל שורה עם `amount_before_vat`/`vat_amount`/`vat_rate` משלה. |
| **מסמך כפול שהועלה פעמיים** | בעת שמירה, המערכת מחפשת תנועה קיימת עם אותו `counterparty_name` + `doc_number` + `amount_total` + `doc_date`. במקרה של התאמה — מוצגת אזהרה במסך האישור עם קישור לתנועה הקיימת; המשתמש בוחר לסמן כ-`is_duplicate_of` (ולא ליצור תנועה חדשה) או לאשר שזו אכן תנועה נפרדת ולהמשיך. |
| **מסמך בלי תאריך ברור** | אם לא זוהה תאריך, `doc_date` נשאר ריק ומוצע (לא ממולא אוטומטית כערך סופי) תאריך ההעלאה כברירת מחדל להשוואה בלבד; שמירת התנועה חסומה עד שהמשתמש מזין תאריך מסמך בפועל. |

---

## 7. הערות טכניות כלליות (ללא מימוש)

- **חילוץ נתונים:** קריאה ל-Anthropic API עם המסמך (תמונה/PDF) ופרומפט קבוע שמחזיר JSON מובנה תואם לשדות `transactions`; תוצאה גולמית נשמרת ב-`documents.extraction_raw` לצורכי ביקורת/דיבוג.
- **Storage:** bucket פרטי ב-Supabase, נתיב לפי `{user_id}/{document_id}`, ללא גישה ציבורית — קריאה רק דרך signed URL למשתמש המחובר.
- **ייצוא לרואה חשבון:** פורמט הקובץ הסופי (PDF מול Excel) לשלושת המסמכים (הוצאות/הכנסות/סיכום) טרם הוחלט — נושא לסגירה בתחילת שלב הייצוא.

---

*המסמך נכתב כאפיון בלבד. ממתין לאישורך לפני תחילת מימוש.*
